# Todo

_Empty._ The previous backlog was converted into tickets in the state repo (`state/tickets/todo/`) via `pb:add` on 2026-06-20. Add new rough ideas here; run `pb:add` to turn them into tickets.
