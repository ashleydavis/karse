import { ErrorsTable } from "../components/errors-table";

// Page showing error conditions occurring in the cluster (Warning events and problem pods).
export function ErrorsPage() {
    return <ErrorsTable />;
}
