import { AllResourcesTable } from "./components/all-resources-table";

// The All resources page: one combined, searchable, sortable, filterable table of
// every resource Karse lists across all kinds in the active context's cluster.
export function AllResourcesPage() {
    return <AllResourcesTable />;
}
