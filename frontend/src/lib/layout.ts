// Shared layout dimensions used across the app shell. Keeping these in one
// place ensures the top bars line up: the main page nav bar (header Toolbar)
// and the left sidebar's top logo bar share the same height.

// Height in pixels of the top bars (header nav bar and sidebar logo bar).
export const TOP_BAR_HEIGHT = 56;
