# Step 1: Write documentation

Write all documentation up front, using the plan (`docs/plans/new/plan-scaffold-and-cluster-overview.md`, section 1, substeps 1-7) as the source of truth. This is documentation-first: the docs describe the feature and conventions as they are intended to work, so the user can read and understand what they are getting before any code is written. The final step revisits every doc to reconcile it against the implementation.

## Files to create

1. **`readme.md`** (repo root): one-paragraph project description (local-only Kubernetes dashboard wrapping `kubectl`); "Requirements" (`kubectl` on `PATH`, `bun` via mise, at least one configured kubeconfig context); "Getting the code" (git clone); "Getting started" (`bun install` from the repo root, `bun run dev` or `bun start` from the repo root); "Documentation" linking the six guide files under `docs/`: `architecture.md`, `api.md`, `e2e-testing.md`, `user-guide.md`, `audit-log.md`, `roadmap.md`.

2. **`claude.md`** (repo root): concise project-level guidance covering purpose/stack/repo-layout/doc-map (root `package.json` for bun workspaces, `backend/`, `frontend/`, `docs/`, `scripts/`); file naming (kebab-case files, PascalCase React identifiers); source layout (everything under `src/`, frontend splits `src/` into `pages/`/`components/`/`lib/`, backend has `src/lib/` for reusable server-side code, backend tests mirror the source tree under `backend/src/tests/`, not co-located); code style (idiomatic TS casing, named exports only, 4-space indent, one statement per line, always-braces, Allman-style `else`); module style (ESM only, never `require`, never dynamic `import`); HTTP (axios via `api-client.ts`); data fetching (React Query everywhere); shared state (`KubeContextProvider`/`useKubeContext()`); tables (`@tanstack/react-table` + MUI); routing (React Router 7, routes in `app.tsx`, pages vs components); icons (Font Awesome, registered in `lib/font-awesome.ts`); backend runtime rules (no Bun-specific APIs, no `*Sync` APIs, async everywhere); testing discipline (every backend non-React module tested under `backend/src/tests/`, `index.ts` exempt, entire frontend including `lib/*.ts` not unit-tested, never `test.skip`/`describe.skip`, always `describe`/`test` never `it`, no fudged assertions); run instructions (`bun run dev` or `bun start` from repo root via bun workspaces; audit log directory set by `KARSE_LOGS_DIR`, default `"../logs"`, resolving to repo root `logs/` with cwd `backend/`); deployment (none, binds 127.0.0.1, no CORS); kubectl assumption; **read-only kubectl invariant** (create/write/edit commands must never be added to the adapter); audit log rule (every kubectl call audited before spawn via the private `kubectl(args)` helper; `LOGS_DIR = process.env.KARSE_LOGS_DIR ?? "../logs"` at module level).

3. **`docs/architecture.md`**: repeat the architecture diagram (the one including `audit-log.ts` and the private `kubectl(args)` audit-then-run path, see plan Architecture section); explain each layer; document 127.0.0.1-only bind; document how kubectl failures surface (adapter throws plain `Error` with kubectl stderr as message; route returns HTTP 500 JSON). Include a **"Local-only threat model / accepted risks"** subsection covering (a) kubectl stderr returned verbatim (issue 24) and (b) no Host-header / DNS-rebinding guard (issue 25). Document the read-only kubectl invariant and the audit log integration (`KARSE_LOGS_DIR` env var, default `"../logs"`, resolving to repo root `logs/` with cwd `backend/`). Do **not** include a type-mirroring section; the shared types package is covered in `docs/development.md`.

4. **`docs/api.md`**: one section per endpoint with request/response types, status codes, curl examples. Endpoints: `GET /api/contexts`, `POST /api/contexts/current` (200 / two 400 messages: empty-or-non-string name and leading-`-` name / 500 on kubectl failure), `GET /api/cluster/overview`, `GET /api/cluster/nodes`. Note routes are local-only and unauthenticated and that kubectl stderr is surfaced verbatim in 500 bodies (accepted tradeoff, issue 24).

5. **`docs/e2e-testing.md`**: user-facing end-to-end test guide. Prerequisites (including `jq`/`curl`/`bash` the smoke script needs, issue 26), start-the-stack, smoke checks, interaction checks, backend-only curl checks, triage list. Companion to `scripts/smoke-tests.sh`. The interaction checks **must include two explicit named manual steps** standing in for the frontend's missing unit coverage (issue 22): **Context-switch refetch** (switch context, confirm both tiles and nodes table refetch) and **Unset-current-context gating** (`kubectl config unset current-context`, confirm "Select a context…" message and that the nodes table fires no request).

6. **`docs/user-guide.md`**: end-user usage guide. What Karse is (and isn't); prerequisites; setup; tour of the cluster home page (header, stat tiles, nodes table, status colours, age computation, sort indicators, search); switching contexts; **audit log** (brief: every kubectl call logged under `logs/<YYYY>/<MM>/<DD>/<HH>.log` at the repo root in local time, retained 3 months, pointer to `docs/audit-log.md`); limitations; troubleshooting; pointers to roadmap/architecture/api.

7. **`docs/roadmap.md`** and **`docs/audit-log.md`**:
   - `roadmap.md`: short intro, then a flat numbered list of at least ten upcoming features (one sentence each), including a **"Host-header allowlist / DNS-rebinding guard"** hardening entry (issue 25). End with an "Already shipped" section listing the cluster overview + nodes view delivered by this plan.
   - `audit-log.md`: title "Karse audit log"; what gets logged (every kubectl invocation, appended to a rolling text file); where (`logs/<YYYY>/<MM>/<DD>/<HH>.log` at the repo root from local time; base directory controlled by `KARSE_LOGS_DIR` env var, default `"../logs"` relative to backend's cwd, resolving to repo root `logs/`; set to an absolute path to override); format (one line per call, local-time ISO 8601 with explicit offset, never `Z`); retention (dated dirs older than 3 months deleted at startup, running-process logs not pruned mid-run); how to read (standard Unix text tools); read-only kubectl by design.

## Verification

No code in this step. Confirm all eight files exist and are internally consistent with the plan. There are no tests to run yet.

## Summary

Created all eight documentation files up front, using the plan as the source of truth:

- `readme.md` (repo root): project description, requirements (`kubectl`, `bun` via mise, a configured context), getting the code (git clone), getting started (optional `mise trust && mise install`, then `bun install` from root, then `bun run dev`), and a Documentation section linking all six guide docs.
- `claude.md` (repo root): purpose/stack/repo-layout/doc-map (root `package.json` bun workspaces); kebab-case filenames with PascalCase React identifiers; source layout (everything under `src/`, frontend `src/` splits into `pages/`/`components/`/`lib/`, backend has `src/lib/` for reusable server-side code, backend tests mirror the tree, not co-located); code style (4-space indent, one statement per line, always-braces, Allman `else`, named exports only); ESM-only module style; HTTP via `api-client.ts`; React Query for all fetching; `KubeContextProvider`/`useKubeContext()`; TanStack Table + MUI; React Router 7; Font Awesome; backend runtime rules (no Bun-specific or `*Sync` APIs); testing discipline (backend-only, `index.ts` exempt, whole frontend incl. `lib/*.ts` not unit-tested, no `.skip`, always `describe`/`test` never `it`, `bun run compile` + `bun run test` each step); run instructions (`bun run dev` from root via bun workspaces; audit log directory via `KARSE_LOGS_DIR`, default `"../logs"` resolving to repo root `logs/`); no deployment / 127.0.0.1 bind; kubectl assumption; read-only kubectl invariant; audit-log rule.
- `docs/architecture.md`: the architecture diagram (including `audit-log.ts` and the private `kubectl(args)` audit-then-run path), per-layer explanation, 127.0.0.1-only bind rationale, how kubectl failures surface (plain `Error` with stderr -> HTTP 500 JSON; version call tolerates failure as `null`), a "Local-only threat model / accepted risks" subsection (verbatim stderr, issue 24; no Host-header guard, issue 25), the read-only kubectl invariant, and the audit-log integration.
- `docs/api.md`: shared type block plus one section per endpoint (`GET /api/contexts`, `POST /api/contexts/current` with both 400 messages and 500, `GET /api/cluster/overview`, `GET /api/cluster/nodes`), each with request/response shapes, status codes, and curl examples; notes routes are local-only/unauthenticated and stderr is verbatim in 500s.
- `docs/e2e-testing.md`: prerequisites (incl. `jq`/`curl`/`bash`, issue 26), start-the-stack, visual smoke checks, the two explicit named manual interaction checks (Context-switch refetch and Unset-current-context gating, issue 22), other interactions, backend-only curl checks, and a triage list.
- `docs/user-guide.md`: what Karse is/isn't, prerequisites, setup, full page tour (header, tiles, nodes table with status colours/age/sort/search), switching contexts, a brief audit-log section pointing to `audit-log.md`, limitations, troubleshooting, and pointers.
- `docs/roadmap.md`: intro, twelve numbered upcoming items (including the Host-header allowlist / DNS-rebinding guard hardening entry, issue 25), and an "Already shipped" section for the cluster overview + nodes view.
- `docs/audit-log.md`: what gets logged, where (`logs/<YYYY>/<MM>/<DD>/<HH>.log` at the repo root, base directory from `KARSE_LOGS_DIR` env var, default `"../logs"` from backend's cwd resolving to repo root), local-time ISO 8601 format with explicit offset (never `Z`), 3-month overflow-free retention pruned at startup (running-process logs kept), how to read with Unix tools, and read-only-by-design.

No code and no tests in this step (per the Verification section). All eight files confirmed present via `ls`. No divergence from the step instructions; the global "no em dashes" style rule was applied throughout. Nothing deferred.
