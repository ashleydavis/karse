# clickable-resource-rows

**ID:** clickable-resource-rows
**Spec:** Settled
**Implementation:** Complete

Every resource table row links to that resource's detail page, so any item in any list is one click away from its full detail view. Applies to nodes, pods, deployments, stateful sets, daemon sets, and the pods listed on detail pages.

## Sub-features
None.
