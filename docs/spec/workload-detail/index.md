# workload-detail

**ID:** workload-detail
**Spec:** Settled
**Implementation:** Complete

A shared detail page for a deployment, stateful set, or daemon set (`/<kind>/:namespace/:name`): metadata, kind-specific status counters, the selected pods, and events. One uniform page across all three workload kinds.

## Sub-features
None.
