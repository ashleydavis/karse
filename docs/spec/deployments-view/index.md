# deployments-view

**ID:** deployments-view
**Spec:** Settled
**Implementation:** Complete

The deployments page (`/deployments`): a read-only, sortable, searchable table of deployments for the active context, scoped to the active namespace or cluster-wide.

## Sub-features
None.
