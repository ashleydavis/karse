# labels-tab

**ID:** labels-tab
**Spec:** Settled
**Implementation:** Complete

A Labels sub tab on each resource detail page that shows that one resource's own labels as a searchable, sortable Key / Value table. Per-detail-page and per-resource: it never aggregates labels across resources.

## Sub-features
None.
