# node-detail

**ID:** node-detail
**Spec:** Settled
**Implementation:** Complete

The node detail page (`/nodes/:name`): conditions, a consumed-vs-free resource usage indicator (cpu, memory, pods), addresses, labels, the pods scheduled on the node (with sortable CPU %/Memory % columns showing each pod's share of the node), and node events.

## Sub-features
None.
