# pod-detail

**ID:** pod-detail
**Spec:** Settled
**Implementation:** Complete

The pod detail page (`/pods/:namespace/:name`): pod metadata, containers and init containers with status and restarts, pod events, and an embedded log viewer.

## Sub-features
None. (The embedded log viewer is specced under `log-viewer`.)
