# labels-modal

**ID:** labels-modal
**Spec:** Settled
**Implementation:** Complete

The shared modal that lists every label on one resource as a searchable, sortable Key / Value table, opened from the truncated Labels cell in any resource table. One component, used by every resource that carries labels.

## Sub-features
None.
