# namespace-detail

**ID:** namespace-detail
**Spec:** Settled
**Implementation:** Complete

The namespace detail page (`/namespaces/:name`): the namespace's phase, labels, annotations, resource quotas and limit ranges; the resources contained in the namespace (searchable, sortable); guided commands; and the raw YAML. Reached by clicking a namespace row.

## Sub-features
None.
