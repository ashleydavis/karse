# daemonsets-view

**ID:** daemonsets-view
**Spec:** Settled
**Implementation:** Complete

The daemon sets page (`/daemonsets`): a read-only, sortable, searchable table of daemon sets for the active context, scoped to the active namespace or cluster-wide.

## Sub-features
None.
