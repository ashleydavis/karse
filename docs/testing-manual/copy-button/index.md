# copy-button manual tests

**Feature:** [copy-button](../../spec/copy-button/index.md)

Manual tests for the shared copy-to-clipboard button: the pod detail page's copy buttons (pod name, namespace, node, Pod IP, container images), the absent-value case where no button is shown, and the Commands and YAML tabs that share the same button and clipboard helper.

## Fixtures
- [20-pod-detail-tabs](../_fixtures-kwok/20-pod-detail-tabs/)
