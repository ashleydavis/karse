# copy-button manual tests

**Feature:** [copy-button](../../spec/copy-button/index.md)

Manual tests for the shared copy-to-clipboard controls: the plain button on single-form values, the two-entry copy menu (short name, full path) on resource names, the absent-value case where no control is shown, the rollout across the detail pages and resource tables, and the Commands and YAML tabs that share the same clipboard helper.

## Fixtures
- [20-pod-detail-tabs](../_fixtures-kwok/20-pod-detail-tabs/)
