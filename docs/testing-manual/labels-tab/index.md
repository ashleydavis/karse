# labels-tab manual tests

**Feature:** [labels-tab](../../spec/labels-tab/index.md)

Manual tests for the per-detail-page Labels tab: a searchable, sortable Key / Value table of a single resource's own labels, on every resource detail page that carries labels (pod, node, namespace, workload). It is not a shared or aggregated list-level tab.

## Fixtures
- [17-raw-yaml-view](../_fixtures-kwok/17-raw-yaml-view/)
